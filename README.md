# Scripts for FFXIV Encounters

An attempt to script the behaviors of FFXIV mechanics.

To serve the scripts, run `python3 serve.py` and it will serve files on
`http://localhost:4001`. You may also provide a custom port number as an
optional argument.
